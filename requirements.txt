# ---- Core dependencies (required to run the full pipeline + tests) ----
numpy>=1.24,<3.0
pandas>=2.0
scikit-learn>=1.3
scipy>=1.10
joblib>=1.3
matplotlib>=3.7
pytest>=7.4

# ---- Optional accelerators / extras (auto-detected; safe to omit) ----
# The code falls back gracefully if these are absent:
#   * SHAP        -> falls back to permutation importance for explainability
#   * LightGBM /  -> fall back to scikit-learn GradientBoosting for regressors
#     XGBoost
#   * DuckDB      -> SQLite is used by default; DuckDB is an alternative engine
# Uncomment to enable:
# shap>=0.44
# lightgbm>=4.0
# xgboost>=2.0
# duckdb>=0.9
