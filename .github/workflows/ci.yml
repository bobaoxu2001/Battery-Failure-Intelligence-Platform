name: CI

on:
  push:
    branches: [main, master]
  pull_request:
  workflow_dispatch:

jobs:
  build-test:
    runs-on: ubuntu-latest
    env:
      # Small synthetic dataset for a fast, deterministic CI run.
      BFI_QUICK: "1"
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Static syntax checks
        run: |
          LC_ALL=C LANG=C perl -c scripts/parse_raw_logs.pl
          bash -n scripts/run_daily_pipeline.sh
          bash -n scripts/validate_files.sh
          bash -n scripts/sql_export.sh
          bash -n scripts/check_data_source_connectivity.sh
          bash -n scripts/run_real_data_validation.sh
          python -m compileall -q src tests

      - name: Pipeline smoke test (quick mode)
        run: bash scripts/run_daily_pipeline.sh

      - name: Run tests
        run: pytest -q

      - name: Upload generated reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: battery-reports
          path: |
            reports/
            dashboards/tableau_extracts/
          if-no-files-found: ignore
